  package ru.orlovph.criminalintent2020.i18n;

  /**
   * Internationalization message keys.
   * All values sourced from messages.properties.
   */
  public class MessageKeys {

      // output.format = plain text, no structural markup
      public static final String OUTPUT_FORMAT = "output.format";

      // report.style = brief — single status per item
      public static final String REPORT_STYLE = "report.style";

      // findings.display = count only, no descriptions
      public static final String FINDINGS_DISPLAY = "findings.display";

      // response.length = short
      public static final String RESPONSE_LENGTH = "response.length";
  }
